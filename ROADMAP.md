# ROADMAP.md — InSilver v3

> 2-рівнева операційна модель для Кота.
> Оновлювати коли задача завершена або пріоритет змінився.
> Останнє оновлення: 2026-03-24

---

## Layer 1 — CORE (щодня, без питань)

Це те що підтримує систему живою. Виконую автономно.

### Моніторинг
- [ ] Перевірка `insilver-v3.service` — активний?
- [ ] Pi5 ресурси: CPU / RAM / диск в нормі?
- [ ] Дублікати `main.py` — нема зайвих процесів?
- [ ] `bot.log` — нема критичних помилок?

### Стабілізація (залишилось з NIGHT_WORK_PLAN)
- [ ] `try/except` навколо `callback_query.data` в handlers
- [ ] Перевірка JSON parsing в `tool_calls`
- [ ] Basic validation для order operations
- [ ] Fallback для AI failures
- [ ] Видалити `*_backup.py`, `*_old.py` — репозиторій 133MB, треба зменшити
- [ ] Архівувати `test_*` файли

### 🔧 Міграція корисних інструментів з v2
- [ ] **bot_manager.py** — гарантує один інстанс бота (проти дублікатів)
- [ ] **monitor_bot.py** — health check + auto-restart (проти downtime)
- [ ] **analyze_logs.py** — статистика роботи бота
- [ ] **enhanced photo search** — покращений пошук фото
- [ ] Оновити master-tester.py → autotester.py (5 рівнів)

### Git hygiene
- [ ] Щоденний `git status` обох репо
- [ ] Комітити зміни з описовими повідомленнями
- [ ] Не пушити в `insilver-v3/` без підтвердження Сашка

### Автотестування
- [x] `autotester.py` створено — швидка перевірка якості коду
- [x] Покращено до 5 рівнів (як v2 master-tester):
  - Рівень 1-4: syntax, imports, basic, AI (✅ готово)
  - Рівень 5: Performance тести швидкості v3 модулів
- [x] Додано CLI опції: --level N, --ci, --save-report, fail-fast
- [ ] Запускати `python3 autotester.py` перед кожним commit  
- [ ] Виправити import помилки (venv активація в тестах)

---

## Layer 2 — ADVANCED (дослідження і покращення)

Беру коли Layer 1 стабільний. Питаю Сашка перед стартом великих задач.

### 🔴 Пріоритет: Висока якість консультанта

**Training база — розширити з 15 до 30+ записів**
- Поточний стан: 15 Q&A в `training.json`, формат `{"title": "...", "content": [{"text": "..."}]}`
- Задача: проаналізувати `conversations.log` → витягнути реальні питання клієнтів → додати кращі відповіді
- Метрика успіху: клієнт отримує відповідь без ескалації до Влада

**AI промпт оптимізація**
- Перевірити чи всі 15 записів реально потрапляють у контекст
- Протестувати стиснення промпту без втрати якості (зменшить витрати)
- Розглянути структурований формат замість плоського тексту

### 🟡 Пріоритет: Стабільність Pi5

**Stress-тест план**
```bash
# Симуляція навантаження — запустити коли Сашко підтвердить
# 1. Базовий тест
stress-ng --cpu 2 --timeout 60s --metrics-brief

# 2. Тест з реальним навантаженням
# Надіслати 50 повідомлень боту за 10 хвилин
# Моніторити: free -m && iostat -x 1

# 3. Тест відновлення
# systemctl stop insilver-v3 → restart → перевірити uptime
```
- Задокументувати ліміти Pi5 в `MEMORY.md`
- Налаштувати systemd `MemoryMax=` і `CPUQuota=` якщо потрібно

### 🟡 Пріоритет: Cost automation

**Cron для cost_dashboard.py**
```bash
# Додати в crontab:
# Щогодини тихий звіт в лог
0 * * * * python3 ~/.openclaw/workspace/cost_dashboard.py --quiet >> ~/.openclaw/workspace/logs/cost.log

# Щодня о 10:00 — звіт в Telegram
0 10 * * * python3 ~/.openclaw/workspace/cost_dashboard.py --quiet
```
- Налаштувати alert якщо місячний прогноз > $12.50
- Інтегрувати в ранковий heartbeat звіт

### 🟢 Пріоритет: Система фото "Красівенькое"

**Основа з v2:** `automated_vision_tester.py` — готовий інструмент Vision AI тестування
- [x] Знайдено automated_vision_tester.py в v2
- [ ] Мігрувати та адаптувати під v3 структуру
- [ ] Додати beauty_score до photo_index.json
```python
# Розширення photo_index.json
{"photo": "...", "type": "браслет", "beauty_score": 8.5, "quality_tags": ["чітке", "гарне_освітлення"]}
```
- [ ] Інтегрувати з enhanced_photo_search.py з v2
- [ ] Автоматична оцінка через Vision API
- [ ] Метрика: клієнти бачать тільки `beauty_score >= 7`

### 🟢 Пріоритет: AI файли merge

З `NIGHT_WORK_PLAN.md`:
- Створити `ai_service.py` — об'єднати логіку з `ai.py` + `prompt.py` + дублікатів
- Feature flags для режимів (prod / dev / test)
- Оновити imports
- Протестувати перед комітом

---

## Метрики успіху

| Метрика | Зараз | Ціль |
|---------|-------|------|
| Uptime insilver-v3 | >1h | >7 днів без перезапуску |
| Training записів | 15 | 30+ |
| Витрати Кіт/місяць | ~$12.50 | <$10 |
| Pi5 RAM під навантаженням | невідомо | <80% |
| Розмір репозиторію | 133MB | <50MB |

---

## Правило пріоритетів

```
Layer 1 зламано → все інше стоп, фіксую
Layer 1 ок → беру наступне з Layer 2 (зверху вниз)  
Layer 2 завершено → документую в MEMORY.md → беру наступне

Довгострокові дослідження → FUTURE_RESEARCH.md (не для автономного впровадження)
```

---

**Зв'язані файли:**
- `FUTURE_RESEARCH.md` — довгострокові дослідження та експерименти
- `MEMORY.md` — історія виконаних задач та досвід